[tool.poetry]
name = "music-recommendation-python"
version = "0.1.0"
description = "Flask API for music recommendations"
authors = ["Anshav Desai <anshavdesai@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.11"
flask = "^3.0.0"
pandas = "^2.2.0"
flask-cors = "^4.0.0"
pymongo = "^4.11.3"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"